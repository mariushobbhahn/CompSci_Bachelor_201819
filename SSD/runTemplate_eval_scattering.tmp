#!/bin/bash

export PYTHONPATH=$${PYTHONPATH}:$${HOME}
python3 ~/SSD/eval_scattering.py --trained_model="weights/${weights_name}.pth" --dataset="${dataset}" --config="${format}" --batch_norm="${batch_norm}"  --pretrained_weights="${pretrained}"
